import { Text } from "~/styles/mixins";
import * as Styled from "./Header.styles";

export const Header = () => {
  return (
    <Styled.Header>
      <Text>TEAMVER</Text>
    </Styled.Header>
  );
};
