import styled from "@emotion/styled";
import { flex } from "~/styles/mixins";

export const ChatRoomTopBar = styled.section`
  ${flex({ align: "center", justify: "between" })}

  height: 55px;
`;

export const ChatMessageWrapper = styled.section``;
export const ChatMessageRight = styled.section``;
export const ChatMessageLeft = styled.section``;
